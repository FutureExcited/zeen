diff --git a/.gitignore b/.gitignore
index 4ae2f4d..ce2dae5 100644
--- a/.gitignore
+++ b/.gitignore
@@ -29,5 +29,16 @@ out/
 !vendor/**/lib/**
 !vendor/**/*.so
 !vendor/**/*.so.*
+vendor/slang/macos-*/
+example-zoro/assets/zoro/*
+!example-zoro/assets/zoro/.gitkeep
+!example-zoro/assets/zoro/README.md
 
-
+example-memory-palace/assets/cards/*
+!example-memory-palace/assets/cards/README.md
+!example-memory-palace/assets/cards/01-yc-s26-tweet.png
+!example-memory-palace/assets/cards/02-company-brain-linkedin.png
+!example-memory-palace/assets/cards/03-ego-survive-goal.png
+!example-memory-palace/assets/cards/04-rebel-devouring-best.png
+!example-memory-palace/assets/cards/05-ego-destiny-isagi.png
+!example-memory-palace/assets/cards/06-yc-superset-browser-use.png
diff --git a/CMakeLists.txt b/CMakeLists.txt
index aa7dc24..6cab033 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -40,6 +40,18 @@ function(be_copy_runtime_deps target)
             COMMAND ${CMAKE_COMMAND} -E copy_if_different
                 "${CMAKE_SOURCE_DIR}/vendor/slang/bin/slang-compiler.dll"
                 "$<TARGET_FILE_DIR:${target}>")
+    elseif(APPLE)
+        set_target_properties(${target} PROPERTIES
+            BUILD_RPATH  "@executable_path;${BE_VULKAN_LIB_DIR}"
+            INSTALL_RPATH "@executable_path;${BE_VULKAN_LIB_DIR}")
+        add_custom_command(TARGET ${target} POST_BUILD
+            COMMAND bash -c "cp -P '${BE_SLANG_LIB_DIR}/'*.dylib '$<TARGET_FILE_DIR:${target}>'"
+            COMMAND ${CMAKE_COMMAND} -E copy_if_different
+                "${BE_MOLTENVK_DYLIB}"
+                "$<TARGET_FILE_DIR:${target}>"
+            COMMAND /usr/bin/install_name_tool -delete_rpath
+                "${BE_SLANG_LIB_DIR}"
+                "$<TARGET_FILE:${target}>")
     else()
         set_target_properties(${target} PROPERTIES
             BUILD_RPATH  "$ORIGIN"
@@ -61,4 +73,6 @@ add_subdirectory(toolkit)
 add_subdirectory(example-game-1)
 add_subdirectory(example-sakura)
 add_subdirectory(example-vulkan)
+add_subdirectory(example-zoro)
+add_subdirectory(example-memory-palace)
 add_subdirectory(devtools/shader-boilerplate-autogen)
diff --git a/CMakePresets.json b/CMakePresets.json
index 9606386..59ebac5 100644
--- a/CMakePresets.json
+++ b/CMakePresets.json
@@ -32,6 +32,22 @@
             "cacheVariables": { "CMAKE_BUILD_TYPE": "Release" },
             "condition": { "type": "equals", "lhs": "${hostSystemName}", "rhs": "Linux" }
         },
+        {
+            "name": "macos-debug",
+            "displayName": "macOS Debug",
+            "inherits": "base",
+            "generator": "Ninja",
+            "cacheVariables": { "CMAKE_BUILD_TYPE": "Debug" },
+            "condition": { "type": "equals", "lhs": "${hostSystemName}", "rhs": "Darwin" }
+        },
+        {
+            "name": "macos-release",
+            "displayName": "macOS Release",
+            "inherits": "base",
+            "generator": "Ninja",
+            "cacheVariables": { "CMAKE_BUILD_TYPE": "Release" },
+            "condition": { "type": "equals", "lhs": "${hostSystemName}", "rhs": "Darwin" }
+        },
         {
             "name": "windows",
             "displayName": "Windows",
@@ -45,6 +61,8 @@
         { "name": "linux-debug",          "displayName": "Linux Debug",          "configurePreset": "linux-debug" },
         { "name": "linux-debug-hotreload", "displayName": "Linux Debug (Hot Reload)", "configurePreset": "linux-debug-hotreload" },
         { "name": "linux-release",   "displayName": "Linux Release",   "configurePreset": "linux-release" },
+        { "name": "macos-debug",     "displayName": "macOS Debug",     "configurePreset": "macos-debug" },
+        { "name": "macos-release",   "displayName": "macOS Release",   "configurePreset": "macos-release" },
         { "name": "windows-debug",   "displayName": "Windows Debug",   "configurePreset": "windows", "configuration": "Debug" },
         { "name": "windows-release", "displayName": "Windows Release", "configurePreset": "windows", "configuration": "Release" }
     ]
diff --git a/core/BeShaderTools.cpp b/core/BeShaderTools.cpp
index e7afb92..7f9215d 100644
--- a/core/BeShaderTools.cpp
+++ b/core/BeShaderTools.cpp
@@ -1,6 +1,7 @@
 #include "BeShaderTools.h"
 
 #include <fstream>
+#include <sstream>
 
 #include "umbrellas/include-libassert.h"
 
@@ -93,4 +94,4 @@ auto BeShaderTools::Split(std::string_view str, const char* delimiters) -> std::
     }
     result.push_back(Take(str, start, str.size()));
     return result;
-}
\ No newline at end of file
+}
diff --git a/core/BeTexture.cpp b/core/BeTexture.cpp
index dcb5aa6..2afa74a 100644
--- a/core/BeTexture.cpp
+++ b/core/BeTexture.cpp
@@ -21,7 +21,8 @@ auto BeTexture::Builder::SetSize      (uint32_t w, uint32_t h)   -> Builder&& {
 auto BeTexture::Builder::SetCubemap   (bool cubemap)             -> Builder&& { _descriptor.IsCubemap = cubemap; return std::move(*this); }
 
 auto BeTexture::Builder::FillWithColor(const glm::vec4& color) -> Builder&& {
-    const auto size = size_t(_descriptor.Width * _descriptor.Height);
+    const auto faceCount = _descriptor.IsCubemap ? size_t(6) : size_t(1);
+    const auto size = size_t(_descriptor.Width * _descriptor.Height) * faceCount;
     const auto data = static_cast<uint8_t*>(malloc(size * 4 * sizeof(uint8_t)));
 
     for (size_t i = 0; i < size; ++i) {
diff --git a/core/BeWindow.cpp b/core/BeWindow.cpp
index 7c2821b..88752ab 100644
--- a/core/BeWindow.cpp
+++ b/core/BeWindow.cpp
@@ -27,6 +27,8 @@ BeWindow::BeWindow(int desiredWidth, int desiredHeight, const std::string& title
 
 #ifdef __linux__
     glfwInitHint(GLFW_PLATFORM, GLFW_PLATFORM_X11);
+#elif defined(__APPLE__)
+    glfwInitHint(GLFW_PLATFORM, GLFW_PLATFORM_COCOA);
 #endif
     if (!glfwInit()) {
         throw std::runtime_error("Failed to initialize GLFW");
diff --git a/core/CMakeLists.txt b/core/CMakeLists.txt
index 2c58e2f..dd357bf 100644
--- a/core/CMakeLists.txt
+++ b/core/CMakeLists.txt
@@ -5,15 +5,13 @@ add_library(core STATIC ${CORE_SOURCES})
 target_include_directories(core
     PUBLIC
         "${CMAKE_CURRENT_SOURCE_DIR}"
-        "${CMAKE_SOURCE_DIR}/vendor"
-        "${CMAKE_SOURCE_DIR}/vendor/slang/include")
+        "${CMAKE_SOURCE_DIR}/vendor")
 
 target_link_libraries(core
     PUBLIC
         glfw
         Vulkan::Vulkan
         libassert::assert
-    PRIVATE
         slang)
 
 be_apply_defaults(core)
diff --git a/devtools/shader-boilerplate-autogen/CMakeLists.txt b/devtools/shader-boilerplate-autogen/CMakeLists.txt
index 35ec6ec..977eaeb 100644
--- a/devtools/shader-boilerplate-autogen/CMakeLists.txt
+++ b/devtools/shader-boilerplate-autogen/CMakeLists.txt
@@ -5,7 +5,9 @@ target_link_libraries(shader-boilerplate-autogen PRIVATE core)
 
 be_apply_defaults(shader-boilerplate-autogen)
 
-add_custom_command(TARGET shader-boilerplate-autogen POST_BUILD
-    COMMAND ${CMAKE_COMMAND} -E copy_if_different
-        $<TARGET_FILE:shader-boilerplate-autogen>
-        ${CMAKE_SOURCE_DIR}/devtools/$<TARGET_FILE_NAME:shader-boilerplate-autogen>)
+if(NOT APPLE)
+    add_custom_command(TARGET shader-boilerplate-autogen POST_BUILD
+        COMMAND ${CMAKE_COMMAND} -E copy_if_different
+            $<TARGET_FILE:shader-boilerplate-autogen>
+            ${CMAKE_SOURCE_DIR}/devtools/$<TARGET_FILE_NAME:shader-boilerplate-autogen>)
+endif()
diff --git a/example-sakura/main.cpp b/example-sakura/main.cpp
index 646c34c..c2da73f 100644
--- a/example-sakura/main.cpp
+++ b/example-sakura/main.cpp
@@ -1,7 +1,33 @@
 #include "Game.h"
 
+#include <filesystem>
+
+#if defined(__APPLE__)
+#include <limits.h>
+#include <mach-o/dyld.h>
+#endif
+
+namespace {
+    auto GetExecutableDirectory() -> std::filesystem::path {
+#if defined(__APPLE__)
+        char buffer[PATH_MAX];
+        uint32_t size = sizeof(buffer);
+        if (_NSGetExecutablePath(buffer, &size) == 0) {
+            return std::filesystem::weakly_canonical(buffer).parent_path();
+        }
+#endif
+        return std::filesystem::current_path();
+    }
+
+    auto MoveWorkingDirectoryToExecutableDir() -> void {
+        std::filesystem::current_path(GetExecutableDirectory());
+    }
+}
+
 int main() {
 
+    MoveWorkingDirectoryToExecutableDir();
+
     const auto game = new Game();
     const auto result = game->Run();
     delete game;
diff --git a/toolkit/assimp-import/BeAssimpImporter.cpp b/toolkit/assimp-import/BeAssimpImporter.cpp
index 3f004c1..0d5552b 100644
--- a/toolkit/assimp-import/BeAssimpImporter.cpp
+++ b/toolkit/assimp-import/BeAssimpImporter.cpp
@@ -1,5 +1,6 @@
 #include "BeAssimpImporter.h"
 
+#include <algorithm>
 #include <ranges>
 #include <assimp/postprocess.h>
 #include <assimp/scene.h>
@@ -146,12 +147,17 @@ auto BeAssimpImporter::LoadTextureFromAssimpPath(
 
 
     if (texPath.C_Str()[0] != '*') {
-        const auto filename = std::filesystem::path(texPath.C_Str()).filename();
+        auto texturePathString = std::string(texPath.C_Str());
+        std::replace(texturePathString.begin(), texturePathString.end(), '\\', '/');
+
+        const auto texturePath = std::filesystem::path(texturePathString);
+        const auto filename = texturePath.filename();
         std::filesystem::path path;
-        if (!std::filesystem::exists(path = parentPath / filename) &&
+        if (!std::filesystem::exists(path = parentPath / texturePath) &&
+            !std::filesystem::exists(path = parentPath / filename) &&
             !std::filesystem::exists(path = parentPath / "textures" / filename) &&
             !std::filesystem::exists(path = parentPath / "images" / filename)) {
-            throw std::runtime_error("Texture file not found: " + filename.string());
+            throw std::runtime_error("Texture file not found: " + texturePathString);
         }
         return builder
             .LoadFromFile(path)
@@ -190,4 +196,3 @@ auto BeAssimpImporter::LoadTextureFromAssimpPath(
     free(converted);
     return resource;
 }
-
diff --git a/toolkit/scenes/BeSceneManager.h b/toolkit/scenes/BeSceneManager.h
index 82834a9..f56480d 100644
--- a/toolkit/scenes/BeSceneManager.h
+++ b/toolkit/scenes/BeSceneManager.h
@@ -7,7 +7,7 @@
 #include <typeinfo>
 #include <umbrellas/include-libassert.h>
 
-class BeScene;
+#include "BeScene.h"
 
 class BeSceneManager {
 private:
diff --git a/vendor/CMakeLists.txt b/vendor/CMakeLists.txt
index 06bc31a..4eaead2 100644
--- a/vendor/CMakeLists.txt
+++ b/vendor/CMakeLists.txt
@@ -39,11 +39,33 @@ add_subdirectory(Assimp EXCLUDE_FROM_ALL)
 # Linux:   place libslang-compiler.so in vendor/slang/linux-x64/lib/
 #          download from https://github.com/shader-slang/slang/releases
 add_library(slang UNKNOWN IMPORTED GLOBAL)
-target_include_directories(slang INTERFACE "${CMAKE_CURRENT_SOURCE_DIR}/slang/include")
 if(WIN32)
+    target_include_directories(slang INTERFACE "${CMAKE_CURRENT_SOURCE_DIR}/slang/include")
     set_target_properties(slang PROPERTIES
         IMPORTED_LOCATION "${CMAKE_CURRENT_SOURCE_DIR}/slang/lib/slang-compiler.lib")
+elseif(APPLE)
+    if(NOT DEFINED BE_SLANG_MACOS_ARCH)
+        if(CMAKE_SYSTEM_PROCESSOR MATCHES "^(arm64|aarch64)$")
+            set(BE_SLANG_MACOS_ARCH "aarch64")
+        else()
+            set(BE_SLANG_MACOS_ARCH "x86_64")
+        endif()
+    endif()
+
+    set(BE_SLANG_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/slang/macos-${BE_SLANG_MACOS_ARCH}" CACHE PATH "Shader Slang macOS binary root")
+    set(BE_SLANG_LIB_DIR "${BE_SLANG_ROOT}/lib" CACHE INTERNAL "Shader Slang runtime library directory")
+
+    if(NOT EXISTS "${BE_SLANG_ROOT}/include/slang.h" OR NOT EXISTS "${BE_SLANG_LIB_DIR}/libslang-compiler.dylib")
+        message(FATAL_ERROR
+            "Missing Shader Slang macOS binaries at ${BE_SLANG_ROOT}. "
+            "Run ./tools/setup-macos-deps.sh from the repository root.")
+    endif()
+
+    target_include_directories(slang INTERFACE "${BE_SLANG_ROOT}/include")
+    set_target_properties(slang PROPERTIES
+        IMPORTED_LOCATION "${BE_SLANG_LIB_DIR}/libslang-compiler.dylib")
 else()
+    target_include_directories(slang INTERFACE "${CMAKE_CURRENT_SOURCE_DIR}/slang/include")
     set_target_properties(slang PROPERTIES
         IMPORTED_LOCATION "${CMAKE_CURRENT_SOURCE_DIR}/slang/linux-x64/lib/libslang-compiler.so")
 endif()
@@ -79,6 +101,28 @@ target_include_directories(Vulkan::Vulkan INTERFACE "${CMAKE_CURRENT_SOURCE_DIR}
 if(WIN32)
     set_target_properties(Vulkan::Vulkan PROPERTIES
         IMPORTED_LOCATION "${CMAKE_CURRENT_SOURCE_DIR}/vulkan-sdk/lib/vulkan-1.lib")
+elseif(APPLE)
+    if(NOT DEFINED BE_HOMEBREW_PREFIX)
+        if(DEFINED ENV{HOMEBREW_PREFIX})
+            set(BE_HOMEBREW_PREFIX "$ENV{HOMEBREW_PREFIX}")
+        elseif(EXISTS "/opt/homebrew")
+            set(BE_HOMEBREW_PREFIX "/opt/homebrew")
+        elseif(EXISTS "/usr/local")
+            set(BE_HOMEBREW_PREFIX "/usr/local")
+        endif()
+    endif()
+    set(BE_HOMEBREW_PREFIX "${BE_HOMEBREW_PREFIX}" CACHE PATH "Homebrew prefix used for Vulkan loader and MoltenVK")
+    set(BE_VULKAN_LIB_DIR "${BE_HOMEBREW_PREFIX}/lib" CACHE INTERNAL "Vulkan runtime library directory")
+    set(BE_MOLTENVK_DYLIB "${BE_VULKAN_LIB_DIR}/libMoltenVK.dylib" CACHE FILEPATH "MoltenVK runtime dylib")
+
+    if(NOT EXISTS "${BE_VULKAN_LIB_DIR}/libvulkan.1.dylib" OR NOT EXISTS "${BE_MOLTENVK_DYLIB}")
+        message(FATAL_ERROR
+            "Missing Homebrew Vulkan loader or MoltenVK under ${BE_HOMEBREW_PREFIX}. "
+            "Install them with: brew install vulkan-loader molten-vk vulkan-headers")
+    endif()
+
+    set_target_properties(Vulkan::Vulkan PROPERTIES
+        IMPORTED_LOCATION "${BE_VULKAN_LIB_DIR}/libvulkan.1.dylib")
 else()
     set_target_properties(Vulkan::Vulkan PROPERTIES
         IMPORTED_LOCATION "${CMAKE_CURRENT_SOURCE_DIR}/vulkan-sdk/lib/linux-x64/libvulkan.so.1")
